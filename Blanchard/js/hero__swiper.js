const swiper = new Swiper('.hero__swiper', {
    // Optional parameter
    loop: true,
    speed: 2000,
    autoplay: {
        delay: 2000,
      },

    effect: 'fade',
    allowTouchMove: false
  });